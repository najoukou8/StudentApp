{% extends 'base.html.twig' %}

{% block title %}Etudiant{% endblock %}

{% block body %}
    <h1>Etudiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ etudiant.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ etudiant.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ etudiant.email }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ etudiant.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_c_r_u_detudiant_index') }}">back to list</a>

    <a href="{{ path('app_c_r_u_detudiant_edit', {'id': etudiant.id}) }}">edit</a>

    {{ include('cru_detudiant/_delete_form.html.twig') }}
{% endblock %}
